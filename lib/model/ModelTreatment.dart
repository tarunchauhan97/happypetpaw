class ModelTreatment {
  int id;
  String title;
  String description;
  String currencyCode;
  double currency;
  String image;

  ModelTreatment(
      this.id, this.title, this.description, this.currencyCode, this.currency,this.image);
}
